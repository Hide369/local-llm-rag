# コーディング対応ライブラリ

チャット画面のサイドバーで「検索対象」を **技術ドキュメント** に切り替えると、
取り込んだ公式ドキュメントを根拠にコード例・Markdown例・Mermaid を答える。
モデル（`gpt-oss:20b`）の学習時点の知識ではなく、取り込んだ記法で答えさせるための
仕組みである。設計の経緯は
[2026-09-12-latest-docs-ingestion-design.md](superpowers/specs/2026-09-12-latest-docs-ingestion-design.md)
にある。

対象の一覧は `docs_sources.toml` が管理する。本書はその中身と、いま実際にDBへ
入っているものを記録する。**数値はすべて実測であり、測っていないものは
「未測定」と書いてある。**

## 1. 取り込み済み（`docs_store.sqlite3`、2026-09-13 時点）

総チャンク数 **47,525**（本文45,170種）。`docs_sources.toml` の10件すべてが入っている。

### `llms-full.txt` から取っているもの（`kind = "llms"`）

| ライブラリ | 記録した版 | チャンク | 取得元 |
|---|---|---|---|
| langchain-text-splitters | 1.1.2 | 14,520 | `docs.langchain.com/llms-full.txt` |
| huggingface_hub | 1.27.0 | 4,913 | `huggingface.co/docs/huggingface_hub/llms-full.txt` |
| mcp | 2.2.0 | 4,862 | `modelcontextprotocol.io/llms-full.txt` |
| streamlit | 1.61.1 | 4,567 | `docs.streamlit.io/llms-full.txt` |
| ollama | （版の固定なし） | 898 | `docs.ollama.com/llms-full.txt` |
| pymupdf | 1.28.2 | 84 | `pymupdf.readthedocs.io/llms-full.txt` |

### GitHub の生 Markdown から取っているもの（`kind = "github"`、2026-09-13 追加）

こちらは**言語そのものの文法**が対象で、ライブラリの資料ではない。1ページ1ファイルで
置くので、出典は `csharp/language-reference/builtin-types/record.md` のように実際の
ページ名で出る。設計は
[2026-09-13-github-docs-ingestion-design.md](superpowers/specs/2026-09-13-github-docs-ingestion-design.md)。

| ソース | ページ | チャンク | 取得元（取り込んだ木のcommit） |
|---|---|---|---|
| csharp | 581 | 11,397 | `dotnet/docs` `docs/csharp/` の10サブパス（434d5f0805） |
| go | 88 | 3,518 | `golang/website` `_content/doc`・`_content/ref`（db07609807） |
| mermaid | 64 | 2,537 | `mermaid-js/mermaid` `packages/mermaid/src/docs`（3f5f7a6781） |
| markdown | 22 | 229 | `github/docs` `content/get-started/writing-on-github`（078b5832ca） |

ページ数は**本文のあるページの数**である。選別したページ数とは一致しない。
フロントマターだけのページ（リダイレクト指定や、目次だけの `index.md`）は
取り込んでも0チャンクにしかならないので書き出さず、`fetch_docs.py` が
「本文なし」として数える。実測 2026-09-13: go 8件、markdown 5件、mermaid 1件。

「記録した版」は `requirements.txt` で固定している版である。取得先は常に最新版の
ドキュメントなので厳密には一致しない。どの版を使っている状態で取り込んだかを
残すために持っている。ollama は Python パッケージとして固定していない（HTTPで
直接叩いている）ため `0.0.0` を入れてある。

### 各資料が実際に扱う範囲

**streamlit**（見出し659、コードフェンス1,877）
インストール、基本概念、全ウィジェット、レイアウト、キャッシュ
（`st.cache_data` / `st.cache_resource`）、セッション状態、マルチページ、
接続とシークレット、テーマ、デプロイ、リリースノート。このリポジトリが使う
`st.dialog` `st.mermaid_chart` `st.cache_resource` はいずれも含まれる。

**langchain-text-splitters**（見出し3,371、コードフェンス6,557）
**コーパス最大で、全体の48.7%を占める。** 資料の範囲はパッケージ名より広い。
`docs.langchain.com` は LangChain 本体・LangGraph・LangSmith をまとめて公開して
おり、H1 を数えると認証・アクセス制御・トレーシング・デプロイまで含む。
このリポジトリが使うのは `langchain-text-splitters` だけだが、取り込んだのは
LangChain のドキュメント全体である。Python と TypeScript の両方の例が載る。

**mcp**（見出し983、コードフェンス940）
MCP サーバーとクライアントの作り方、アーキテクチャ、トランスポート（stdio /
HTTP）、バージョニング、各言語の SDK、デバッグ、MCP Inspector。このリポジトリは
`scripts/rag_mcp_server.py` で MCP サーバーを提供しているため入れてある。
ドメインは `modelcontextprotocol.io` で、PyPI のパッケージ名とは無関係である。

**huggingface_hub**（見出し295、コードフェンス3,436）
インストール、認証、リポジトリの作成、ファイルのダウンロードとアップロード、
HfApi クライアント、キャッシュ、環境変数、CLI、例外、Xet。

**ollama**（見出し232、コードフェンス538）
API エンドポイント（`/api/chat` `/api/embed` `/api/generate`）、モデルの作成・
複製・削除、埋め込み、ストリーミング、認証と API キー、エラー、Anthropic API
互換、クライアントライブラリ。このリポジトリが `ingest/chat.py` と
`ingest/embedder.py` で叩いている API がここに載っている。

**pymupdf**（見出し17、コードフェンス64）
インストール、Document / Page クラス、テキスト抽出の各形式、PyMuPDF4LLM、
Pixmap、注釈、描画、ジオメトリ、CLI。**他の5件に比べて極端に小さい**
（22,852バイト）。`llms-full.txt` が概要止まりで、API リファレンス全体を
含んでいないためである。細かい引数まで尋ねる用途には足りない。

**csharp**（581ページ、4,717,411バイト、コードフェンス5,769）
`language-reference`（328ページ）を軸に、programming-guide、fundamentals、linq、
advanced-topics、tour-of-csharp、whats-new、非同期、チュートリアル。**コンパイラの
エラー番号ごとのページ（`misc`、409ファイル）は入れていない。**

MS Learn 形式はコード例を本文に書かず `:::code` で外部の `.cs` を参照する。取得時に
解決して本文へ差し込んでいる（`scripts/code_references.py`）。実測 2026-09-13:
1,638件の参照のうち **1,484件（90.6%）を解決**。残り154件は、木に存在しないパスを
指すもの52件と、印が一致しないものである。この154件は報告に出る（`fetch_docs.py` の
実行結果）。この処理の結果、コードフェンスを1つも
持たないページは標本62%から**実測21%（121/581）**へ下がった。

**go**（88ページ、1,887,108バイト、コードフェンス637）
**言語仕様は入っていない。** `spec.html` と Effective Go は HTML であり、Markdown
では取れないためである（設計書4.4節）。入っているのは各版のリリースノート
（`go1.1.md`〜`go1.28.md` の29ファイル）、FAQ、modules リファレンス、security、
database ガイド、チュートリアル。**「各版で何が増えたか」には答えられるが、文法の
網羅的な参照にはならない。** 使い方は4節に実測つきで書いてある。

**mermaid**（64ページ、731,917バイト、コードフェンス1,949）
**コードフェンスの密度が最も高い。** フローチャート、シーケンス図、クラス図、状態図、
ER図、ガント、円グラフ、マインドマップなど各記法と設定。フェンスを持たないページは
64件中8件しかない。

**markdown**（22ページ、87,199バイト、コードフェンス106）
GitHub Flavored Markdown の書き方。見出し、表、コードブロック、脚注、タスクリスト、
Mermaid の埋め込み、警告記法。**最も小さい**（229チャンク）。CommonMark の仕様書は
入れていない（例が独自の区切り文字で書かれており、現在のチャンカーが扱えない）。

## 2. 取り込み待ち

**現在なし。** `docs_sources.toml` の10件はすべて `docs_store.sqlite3` に反映済み
である。反映には Ollama への接続（埋め込み）が要るので、追加したときはこの節に
「未取り込み」として残し、埋め込みを走らせてから1節へ移すこと。

実測した所要時間は3回分ある（いずれも NVIDIA L4、ngrok 経由）。

| 日付 | 対象 | チャンク | 所要 |
|---|---|---|---|
| 2026-09-12 | streamlit / huggingface_hub / ollama / pymupdf ほか | 11,993 | 724秒 |
| 2026-09-13 | langchain-text-splitters / mcp | 19,382 | 1,532秒 |
| 2026-09-13 | csharp / go / mermaid / markdown（758ファイル） | 17,676 | 2,417秒 |

差分取り込みは効いている。2回目は6ファイル中4ファイルが「変更なし」でスキップ
され、新しい2件だけが処理された。3回目も既存6件はすべてスキップされている。

**ファイル数が多いと遅くなる。** 3回目は12.65チャンク/秒だった2回目に対して
**7.31チャンク/秒**で、ほぼ半分である。チャンク数はむしろ少ないのに時間がかかった。
1ページ1ファイルで758ファイルを処理したためで、原因は埋め込みの往復回数にある
（実測 2026-09-13: 埋め込みは1件でも8件でも1回あたり約640ms。`EMBED_BATCH_SIZE`
は8で、16GB環境で決めた値である）。**GPUの速さではなく往復の回数が効いている。**

## 3. 対象にできないもの

`llms-full.txt` を公開していないライブラリは、`kind = "llms"` では対象にできない。
GitHub に生の Markdown があるものは `kind = "github"` で取れるようになった
（[2026-09-13-github-docs-ingestion-design.md](superpowers/specs/2026-09-13-github-docs-ingestion-design.md)）。
Web クローリングは却下したままである。

### Java（GitHub にも公式の Markdown が無い）

実測 2026-09-13: `dev-java/dev.java` と `java-tutorials/java-tutorials` はいずれも
404 である。Spring は実在するが **AsciiDoc** であり
（`spring-projects/spring-framework` 471ファイル/3.07MB、
`spring-projects/spring-boot` 218ファイル/1.77MB）、パーサーの追加が要るうえ、
それは言語の文法ではなくフレームワークの資料である。今回は見送った。

### `llms-full.txt` が無く、GitHub にも適した資料が無いもの

実測 2026-09-12〜13 で 404 を返したもの:

| ライブラリ | このリポジトリでの用途 |
|---|---|
| python-pptx | PowerPoint の解析 |
| python-docx | Word の解析 |
| openpyxl | Excel の解析 |
| pillow | 画像処理 |
| requests | HTTP |
| pytest | テスト |
| numpy | ベクトル演算 |
| onnxruntime | OCR とリランカーの推論 |
| fastapi | （未使用。参考文書が例に挙げていたので確認した） |

`rapidocr` `tokenizers` `python-dotenv` は未確認である。

これらについて質問するときは、サイドバーを **社内資料** のままにするか、
技術ドキュメントを選んだうえで「ドキュメントに無い」と答えが返ることを
前提にすること。取り込んでいない以上、モデルは学習時点の知識で答えるか、
答えられないと言うかのどちらかになる。

## 4. 実測された品質と使い方

### 日本語で質問してよい

質問は内部で英語の検索クエリに訳してから検索する（`ingest/query_translation.py`）。
**回答は元の日本語の質問から生成する**ので、訳が外れても回答が壊れることはなく、
検索の精度が落ちるだけである。

この仕組みが要る理由は実測にある。取り込んだ資料は英語だが、検索の仕組み
（文字bigramのBM25、日本語で調整したしきい値）は社内資料向けに作られているため、
日本語のまま検索すると外す。

| 質問 | 訳された検索クエリ | Reranker | 答えたAPI |
|---|---|---|---|
| Streamlitのキャッシュはどう書く？ | `Streamlit st.cache_data usage` | 0.06 → **5.68** | `st.cache_data` / `st.cache_resource` |
| Streamlitでモーダルダイアログを出す書き方を教えて | `Streamlit modal dialog st.dialog example` | 3.40 | `st.dialog`（現行シグネチャ） |
| Streamlitでマーメイドの図を描くには | `Streamlit Mermaid diagram ...` | 0.77 | `st.mermaid_chart` |

翻訳を入れる前は、1行目の質問が CLI の `streamlit cache clear` を引き、モデルは
（渡された根拠に照らして正しく）「ドキュメントには記述がありません」と答えていた。

### API 名を書くと強い

同じ内容でも、API 名を含めた方が検索が当たる。実測（2026-09-12、Streamlit）:

- `st.cache_data`（API名）→ Reranker **7.02**
- `How do I cache data in Streamlit?`（英語の文）→ **5.73**
- `Streamlitのキャッシュはどう書く？`（日本語の文）→ **0.06**

分かっている API 名があれば質問に入れるとよい。翻訳を挟んでも同じで、上の
langchain の例のように、訳がクラス名まで届かないと外す。

### langchain と mcp（2026-09-13 追加分）

翻訳・ベクトル検索・BM25・リランカーを通しで実測した（`gpt-oss:20b`、
NVIDIA L4、ngrok 経由）。

| 質問 | 訳された検索クエリ | Reranker | 引いた内容 |
|---|---|---|---|
| MCPのstdioトランスポートでクライアントを作るには | `MCP stdio transport create client` | **4.62** | MCP クライアントの作り方、参加者の構成 |
| MCPサーバーでツールを定義する書き方 | `MCP server tool definition` | **3.33** | MCP サーバーの役割とツール定義 |
| LangGraphでグラフのノードとエッジを定義するには | `LangGraph define graph nodes edges` | **1.94** | `Define a new graph` の例、`graph.compile()` |
| LangChainでテキストを分割する書き方を教えて | `LangChain TextSplitter usage` | **-0.09** | `pip install` の行（**外した**） |

**最後の1件は外している。** 引いたのはインストールコマンドで、分割のコードでは
ない。同じ内容でも API 名を含む `LangChain RecursiveCharacterTextSplitter split
text example` で引くと、`PyMuPDFLoader` と分割の実例が出る（BM25とリランカーのみ
で1.54）。訳が `TextSplitter` までしか出せず、`RecursiveCharacterTextSplitter`
という実際のクラス名に届かなかったことが原因である。

これは下の「API 名を書くと強い」の2つ目の実例になっている。**翻訳はクラス名まで
は当てられないことがある。** 分かっている API 名は質問に書いた方がよい。

### csharp・go・mermaid・markdown（2026-09-13 追加分）

翻訳・ベクトル検索・BM25・リランカーを通しで実測した（`gpt-oss:20b`、NVIDIA L4、
ngrok 経由）。

| 質問 | 訳された検索クエリ | Reranker | 引いた出典 |
|---|---|---|---|
| C#のパターンマッチングの構文 | `C# pattern matching syntax` | **6.98** | `csharp/fundamentals/functional/pattern-matching.md` |
| マーメイドでフローチャートを書く記法 | `Mermaid flowchart syntax` | **5.10** | `mermaid/syntax/flowchart.md` |
| C#のrecord型の書き方を教えて | `C# record type syntax` | **4.52** | `csharp/language-reference/builtin-types/record.md` |
| Markdownで表を書く書き方 | `Markdown table syntax` | **4.36** | `markdown/.../quickstart-for-writing-on-github.md` |
| Goのジェネリクスはどの版で入りましたか | `Go generics introduced in which version` | **2.52** | `go/doc/faq.md`（**外した**） |

上4件はいずれも該当ページそのものを引いている。出典がページ単位で出るので、どの
資料を根拠にしたかが読み取れる。

### Go は版番号を入れると当たる

上表の最後の1件は外している。引いたのは FAQ の「Goに機能Xが無くて気になるなら」と
いう無関係な段落だった。**内容が無いわけではない。** `go1.18.md` はコーパスに入って
おり、`type parameter` を含むチャンクは385件、`generic` を含むチャンクは869件ある。

クエリの形だけを変えて測ると、こうなる（翻訳は挟まず直接検索）。

| 検索クエリ | Reranker | 引いたページ |
|---|---|---|
| `Go generics introduced in which version` | 2.52 | `go/doc/faq.md`（外し） |
| **`Go 1.18 generics type parameters`** | **4.41** | **`go/doc/go1.18.md`（的中）** |
| `Go type parameters release notes` | 1.82 | `go/doc/faq.md`（外し） |

**版番号が入ると当たる。** Go のコーパスはリリースノートが主体（1節）なので、
「どの版で入ったか」を尋ねる形より「1.18 で何が入ったか」を尋ねる形のほうが資料の
構造に合う。下の「API 名を書くと強い」と同じことが、Go では版番号で起きている。

### 弱いところ

- **Go は文法の参照にならない。** 言語仕様が HTML なので入っていない（1節）。
  リリースノート主体なので、版番号を入れない質問は外しやすい（上の実測 2.52）。
- **「Streamlit で Mermaid を描く」は依然として弱い**（Reranker 0.77）。これは
  Streamlit 側の資料を引く質問であり、Mermaid の記法そのものを尋ねる質問
  （5.10）とは別である。専用のコーパスが入ったのは後者だけ。
- **markdown は229チャンクと小さい。** GitHub の書き方ガイドであって、
  CommonMark の仕様書ではない。
- **pymupdf は資料そのものが薄い。** 概要止まりで API リファレンスを含まない。
- **本文に出典は出ない。** 出典は回答の下の「参考にした情報」を開くと出る。
  実測10回の回答すべてでファイル名は本文に出なかった（`gpt-oss:20b` のみで確認）。

## 5. ライブラリを追加する

`docs_sources.toml` に足して、取得と取り込みを走らせる。

```bash
python -m scripts.fetch_docs
python -m scripts.ingest_source --source-dir docs_source \
    --db docs_store.sqlite3 --keep-code-blocks
```

GitHub から取る場合は `kind = "github"` を書く。`llms-full.txt` を公開していない
対象（言語そのものの文法など）はこちらになる。

```toml
[[source]]
name = "csharp"
kind = "github"
repo = "dotnet/docs"
ref = "main"                     # 既定ブランチを明示する。golang/website は
paths = ["docs/csharp/linq"]     # master、mermaid-js/mermaid は develop である
resolve_code_refs = true         # 既定 false。MS Learn の :::code を解決する
version = "0.0.0"
```

未認証で足りる（Trees API はリポジトリにつき1回、未認証枠は60回/時）。
**トークンは要らない。**

追加するときの注意が2つある。

**`llms.txt` ではなく `llms-full.txt` の URL を書くこと。** `llms.txt` は各ページへの
リンクの目次であって本文ではない（実測 2026-09-12: `docs.streamlit.io/llms.txt` は
66,927バイトあるがコードフェンスは**0個**）。目次を取り込むと、検索は当たるのに
答えが書けないという診断しにくい状態になる。

**ドキュメントのドメインはパッケージ名から推測できない。** 実例:

- `mcp` → `modelcontextprotocol.io`（PyPI 名と無関係）
- `langchain-text-splitters` → `docs.langchain.com`（`python.langchain.com` は HTML を返す）
- `huggingface_hub` → `huggingface.co/docs/huggingface_hub/`（サブパス）

URL を間違えても、サーバーが 200 で HTML を返すことがある。`fetch_docs.py` は
本文の先頭と `Content-Type` の両方を見て HTML を弾き、`NotDocumentationError` を
投げて1件の失敗として報告する（他のライブラリの取得は続く）。本文中に HTML の例が
出てくる正しい資料は捨てない（先頭だけで判定しているため）。

この歯止めは実害があって入れた。`python.langchain.com/llms-full.txt` は 404 では
なく、ドキュメントサイトの HTML を **200 で** 返す（実測 2026-09-13）。状態コード
しか見ていなかったため素通りし、HTML・CSS・JavaScript が **1,531チャンク**
（当時のコーパス11,993件の12.8%）DB に入っていた。汚染分は削除済みである。
この失敗は静かで診断が遠い。取り込みは成功と表示され、検索も当たり、ただ答えが
書けないノイズが混ざるだけだからである。

## 6. 更新

取得は内容の SHA-256 を前回と比べ、変わったものだけ書き出す。変わっていなければ
「変更なし」と報告してファイルを書き換えないので、取り込みも走らない。

`kind = "github"` は **1ページ1ファイル**で置くので、差分もページ単位で効く。
1ページ直っても再埋め込みは1ページ分で済み、出典も実際のページ名で出る。
フロントマターには木のコミット SHA が入るので、どの時点から取ったかが後から分かる
（`llms-full.txt` では取れない再現性である）。

定期実行はしない。人が `scripts/fetch_docs.py` を起動したときだけ外部へ通信する
（`AGENTS.md` の「外部ドキュメントの参照」参照）。

`docs_source/` と `docs_store.sqlite3` は `.gitignore` 済みである。
`source/` と `vector_store.sqlite3` と同じく、いつでも取り直せるためである。
追跡するのは設定ファイル `docs_sources.toml` だけ。
