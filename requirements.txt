# 本システムが直接importするパッケージだけを、myvenv313 に入っている実績のある
# バージョンで固定する。推移的な依存は挙げない。
# 完全な再現が要る場合は `pip freeze > requirements.lock.txt` を別途取ること。
#
# 各パッケージの用途と使用箇所は docs/依存関係一覧.md を参照。
# ここを変更したら docs/依存関係一覧.md の表も必ず更新すること。

# 文書の解析
pymupdf==1.28.2
python-pptx==1.0.2
python-docx==1.2.0

# OCR（画像PDF）とリランカーの推論。onnxruntimeは両方で使う
rapidocr==3.9.2
onnxruntime==1.28.0

# リランカーのモデル取得とトークナイズ。以前はchromadbが依存として持ち込んでいたが、
# ingest/reranker.py が直接importする以上、上流の都合で黙って壊れないよう明示する
huggingface_hub==1.27.0
tokenizers==0.23.1

# リランカーのスコア計算と、ベクトルストアの総当たり検索（ingest/vector_store.py）
numpy==2.5.2

# チャンク分割・UI。ベクトルの保管は標準ライブラリの sqlite3 で行うため依存は増えない
langchain-text-splitters==1.1.2
streamlit==1.61.1

# Ollamaへのアクセスと設定
requests==2.32.3
python-dotenv==1.2.2

# テスト
pytest==9.1.1
pillow==12.3.0
